{
  "name": "patient-vitals-lambda",
  "version": "1.0.0",
  "description": "Lambda function for patient vital ingestion and Salesforce sync",
  "main": "index.js",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "jsonwebtoken": "^9.0.2"
  },
  "scripts": {
    "test": "echo \"No tests configured\"",
    "deploy": "aws lambda update-function-code --function-name patient-vitals --zip-file fileb://function.zip"
  }
}